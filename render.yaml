services:
  - type: web
    name: future-dashboard
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/storage
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
